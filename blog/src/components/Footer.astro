---
import { X_URL } from '../consts';

const today = new Date();
---

<footer>
	<div class="footer-wrap">
		<p>&copy; {today.getFullYear()} Midage Dev Notes</p>
		<p class="small">
			X에 남긴 실무 판단을 선별해 보존하는 엔지니어링 노트.
			<a href={X_URL} target="_blank" rel="noreferrer">@midagedev</a>
		</p>
	</div>
</footer>
<style>
	footer {
		padding: 2.5rem 1rem 4rem;
		color: rgb(var(--ink-dim));
	}
	.footer-wrap {
		max-width: 1080px;
		margin: 0 auto;
		border-top: 1px solid rgba(var(--line), 0.8);
		padding-top: 1rem;
	}
	.small {
		font-size: 0.92rem;
		margin-top: 0.35rem;
	}
	.small a {
		font-weight: 700;
	}
</style>
